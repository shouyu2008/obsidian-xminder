{
  "name": "obsidian-xminder",
  "version": "1.0.0",
  "description": "Read, write and embed XMind files in Obsidian",
  "main": "main.js",
  "scripts": {
    "dev": "NODE_OPTIONS='' node esbuild.config.mjs",
    "build": "NODE_OPTIONS='' tsc --noEmit --skipLibCheck && NODE_OPTIONS='' node esbuild.config.mjs production",
    "build:clean": "rm -rf dist && npm run build",
    "typecheck": "NODE_OPTIONS='' tsc --noEmit --skipLibCheck",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "xmind",
    "mind-map"
  ],
  "license": "MIT",
  "dependencies": {
    "jszip": "3.10.1",
    "mind-elixir": "^4.3.3"
  },
  "devDependencies": {
    "@types/node": "^16.11.6",
    "builtin-modules": "3.3.0",
    "esbuild": "0.21.5",
    "obsidian": "latest",
    "tslib": "2.6.2",
    "typescript": "5.4.5"
  }
}
